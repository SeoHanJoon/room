import React, { Fragment } from 'react';
import { Headers, Mains, Boards } from '../../components';

const Main = () => (
  <Fragment>
    <Headers />
    <Mains />
    <Boards />
  </Fragment>
);

export default Main;
